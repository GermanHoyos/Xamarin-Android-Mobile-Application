<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="DegreePlanner.Pages.A_TermDelete">
	<StackLayout>
		<Grid RowSpacing="0" ColumnSpacing="0">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*" />
				<ColumnDefinition Width="*" />

			</Grid.ColumnDefinitions>
			<Grid.RowDefinitions>
				<RowDefinition Height="50" />
				<RowDefinition Height="50" />

			</Grid.RowDefinitions>
			<Label	Text="Delete Terms" 
					Grid.Column="0"
					Grid.Row="0"
					Grid.ColumnSpan="3"
					BackgroundColor="PowderBlue"
					TextColor="white"
					FontSize="Title"
					HorizontalTextAlignment="Center"
			></Label>
			<Button	Text="Save"
					Grid.Column="0"
					Grid.Row="1"
					Grid.RowSpan="1"
					Clicked="DeleteTermSave"
			/>
			<Button	Text="Cancel"
					Grid.Column="2"
					Grid.Row="1"
					Grid.RowSpan="1"
					Clicked="DeleteTermCancel" 
			/>
		</Grid>
		<Entry	x:Name="titleInput"
				Text="Enter term title for deletion"
				HorizontalTextAlignment="Center"
		/>



	</StackLayout>
</ContentPage>